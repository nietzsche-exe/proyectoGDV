package util;

/**
 * Clase que almacena en un Enum 3 posibeles elecciones de genero por el usuario a la 
 * hora de registrarse o si quiere cambiarlo en la configuracion de su perfil
 */
public enum Genero {
	Masculino,
    Femenino,
    Otro
}
